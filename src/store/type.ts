export default interface stateType {
  name: string,
  token: string,
  password: string,
  permissionList: Array<object>,
  router: Array<object>,
  total: number,
  record: number,
  buildName:string
}
